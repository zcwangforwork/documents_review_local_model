# 会话上下文记录

**保存时间:** 2026-06-12 10:40:34
**启动目录:** E:\nrf_sample_codes\working_team_work\public\project\project_0428_beta\backend
**保存路径:** E:\nrf_sample_codes\working_team_work\public\project\project_0428_beta\backend\docs\20260612_104034_context.md

## 背景

本会话是上一个被截断会话的延续。用户正在将 `project_0428_beta` 医疗设备文档审核系统从线性 RAG 管线改造为 LangGraph Agent 系统。

## 对话详情

### 用户:
（从上下文恢复的会话继续，未发出新消息）

### AI:
继续执行 Task #8：**Refactor graph: pre-retrieve in plan_strategy, remove per-section tool loop**

AI 读取了三个关键文件的当前状态：
- `backend/agent_graph.py` (667 行)
- `backend/agent_state.py` (70 行)
- `backend/main.py` (1493 行)

**问题诊断**: `audit_section_node` 通过 `llm.bind_tools(tools)` 创建 agent 循环，每个小节消耗 ~10 个 super-steps。41 个小节 × 10 = 410+ 步，超出任何合理的 recursion_limit。

**解决方案**: 预检索 + 直接 LLM 审核（无工具循环）

实施了三文件修改：

#### 1. `agent_state.py` — 添加 `knowledge_context: str` 字段
存储 plan_strategy 预检索的法规上下文，供 audit_section 直接使用。

#### 2. `agent_graph.py` — 重写核心
- `plan_strategy_node(state, llm, retriever)`: LLM 制定审核策略后，程序化调用 `retriever.search_sync()` 为每个相关标准（ISO 14971, IEC 62304 等）预检索法规条款，存入 `knowledge_context`
- `audit_section_node(state, llm)`: 移除 `tools` 参数和 `bind_tools`，改为直接 LLM 调用，使用预检索的 `knowledge_context`
- 移除: `ToolNode`, `route_after_audit`, `tools` 节点, `audit_section ↔ tools` 条件边
- 新拓扑: `plan_strategy → audit_section → evaluate_result → (reaudit) → update_context → next/done → cross_validate → generate_report → END`
- 每节 super-step 从 ~10 降至 ~3

#### 3. `main.py` — 三点修改
- 移除 `MAX_SECTIONS = 30` 截断（按用户明确要求）
- `compile_agent_graph(llm=agent_llm, retriever=rag_retriever)` — 传递 retriever 替代 tools
- `recursion_limit`: `max(200, 25 + len(sections) * 5)` — 41 节约需 230 步

验证：Graph 成功编译，6 个节点（无 tools 节点）。

---

### 用户:
Agent 审核失败 审核超时（超过10分钟），请尝试更小的文档或减少审核章节数

### AI:
诊断两个问题：
1. `evaluate_result_node` 每个小节仍有一次独立 LLM 调用（41 个额外调用）
2. 前端超时仅 10 分钟

**LLM 调用统计**: plan_strategy(1) + audit_sections(41) + evaluate_results(41) + cross_validate(1) + report(1) = 85 次调用。每次 ~15-20s，总计 ~21 分钟。

**修复**: 消除 evaluate_result 的 LLM 调用

#### `agent_state.py` — 添加字段
```python
confidence: int
key_findings: List[str]
```

#### `agent_graph.py` — 重写 audit_section_node 和 evaluate_result_node
- `audit_section_node`: 要求 LLM 在响应末尾附加 JSON 元数据块 `{"confidence": <1-5>, "key_findings": [...]}`。解析后返回 `confidence` 和 `key_findings` 到 state
- `evaluate_result_node(state)` → **纯解析函数**，不再接收 `llm` 参数，不发起 LLM 调用。直接从 state 读取 confidence/key_findings，构建 audit_results 条目

**LLM 调用数降至**: N+3（41 节约 44 次），估算 11-15 分钟。

#### `frontend/agent.html` — 超时调整
- 首次改为 30 分钟（1800000ms）
- 用户要求改为 15 分钟（900000ms）
- 用户再次要求改回 30 分钟（1800000ms）

最终前端超时: **30 分钟**。

---

## 关键信息

- **项目路径**: `E:\nrf_sample_codes\working_team_work\public\project\project_0428_beta\backend`
- **核心架构**: LangGraph StateGraph + ChatOpenAI(deepseek-v4-pro via 火山方舟)
- **图拓扑**: `plan_strategy → audit_section → evaluate_result(纯解析) → update_context → next/done → cross_validate → generate_report → END`
- **不截断章节**: 用户明确拒绝 MAX_SECTIONS=30 截断方案
- **前端超时**: 30 分钟（1800000ms）
- **每节 super-step 消耗**: audit_section(1) + evaluate_result(1) = 2 步（不含 reaudit）
- **LLM 调用总数**: N+3（N = 章节数）

## 涉及文件

| 文件 | 状态 | 修改内容 |
|------|------|----------|
| `backend/agent_state.py` | 已修改 | 添加 `knowledge_context`, `confidence`, `key_findings` 字段 |
| `backend/agent_graph.py` | 已重写 | 预检索 + 直接LLM审核 + 纯解析evaluate |
| `backend/main.py` | 已修改 | 移除截断, 传递retriever, 更新recursion_limit |
| `frontend/agent.html` | 已修改 | 超时从10分钟改为30分钟 |
| `backend/agent_tools.py` | 未修改 | 工具仍存在但不再被图使用 |

## 决策记录

1. **消除 per-section tool 循环**: 改为预检索方案。LLM 在 plan_strategy 中调用 retriever.search_sync() 程序化检索法规，audit_section 直接使用预检索上下文，不再 bind_tools
2. **消除 per-section evaluate LLM 调用**: audit_section 输出结构化 JSON 自评 confidence/key_findings，evaluate_result 改为纯解析函数
3. **不截断章节**: 按用户要求移除 MAX_SECTIONS=30 限制
4. **前端超时 30 分钟**: 经 10→30→15→30 的调整，最终确定为 30 分钟
